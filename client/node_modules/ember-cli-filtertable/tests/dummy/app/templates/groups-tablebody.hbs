<td><span class="spec-toggle-expand" {{action "toggleExpand" r }}>{{tree-tab r}}</span>
    <strong>{{r.name}}</strong>
    {{#if r.hasChildren}}<span class="label label-primary">{{r.childNum}} children</span>{{/if}}
</td>
